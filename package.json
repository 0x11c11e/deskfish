{
  "name": "deskfish",
  "displayName": "Deskfish",
  "description": "Give your AI its own computer. A sandboxed Linux desktop in a VS Code tab, an agent that works it like a human, any LLM via API — and it knocks on the glass when it needs you.",
  "version": "0.1.0",
  "publisher": "ImanReihanian",
  "license": "Apache-2.0",
  "private": true,
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/0x11c11e/deskfish.git"
  },
  "homepage": "https://deskfish.sh",
  "bugs": {
    "url": "https://github.com/0x11c11e/deskfish/issues"
  },
  "keywords": [
    "ai",
    "agent",
    "computer-use",
    "desktop",
    "sandbox",
    "podman",
    "browser-automation"
  ],
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Other"
  ],
  "main": "./dist/extension.js",
  "bin": {
    "deskfish": "dist/cli.js"
  },
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "deskfish",
          "title": "Deskfish",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "deskfish": [
        {
          "type": "webview",
          "id": "deskfish.chat",
          "name": "Chat",
          "icon": "media/icon.svg"
        }
      ]
    },
    "commands": [
      {
        "command": "deskfish.openDesktop",
        "title": "Deskfish: Open Desktop View",
        "icon": "$(device-desktop)"
      },
      {
        "command": "deskfish.changeModel",
        "title": "Deskfish: Choose Model…"
      },
      {
        "command": "deskfish.setApiKey",
        "title": "Deskfish: Set LLM API Key"
      },
      {
        "command": "deskfish.toggleDesktop",
        "title": "Deskfish: Turn Desktop On / Off",
        "icon": "$(vm)"
      },
      {
        "command": "deskfish.startDesktop",
        "title": "Deskfish: Turn Desktop On"
      },
      {
        "command": "deskfish.stopDesktop",
        "title": "Deskfish: Turn Desktop Off"
      },
      {
        "command": "deskfish.restartDesktop",
        "title": "Deskfish: Restart Desktop",
        "icon": "$(debug-restart)"
      },
      {
        "command": "deskfish.stopAgent",
        "title": "Deskfish: Stop Agent",
        "icon": "$(debug-stop)"
      },
      {
        "command": "deskfish.newChat",
        "title": "Deskfish: New Chat",
        "icon": "$(add)"
      },
      {
        "command": "deskfish.editMemory",
        "title": "Deskfish: Edit Memories (facts)"
      },
      {
        "command": "deskfish.clearMemory",
        "title": "Deskfish: Forget All Memories (facts)"
      },
      {
        "command": "deskfish.showLog",
        "title": "Deskfish: Show Log"
      },
      {
        "command": "deskfish.saveFile",
        "title": "Deskfish: Save a File from the Desktop…"
      },
      {
        "command": "deskfish.installRuntime",
        "title": "Deskfish: Install Podman…"
      },
      {
        "command": "deskfish.openDocs",
        "title": "Deskfish: Open Documentation",
        "icon": "$(book)"
      },
      {
        "command": "deskfish.showSelf",
        "title": "Deskfish: Who She Is"
      },
      {
        "command": "deskfish.openJournal",
        "title": "Deskfish: Open Her Journal"
      },
      {
        "command": "deskfish.reflect",
        "title": "Deskfish: Let Her Reflect"
      },
      {
        "command": "deskfish.scheduleTask",
        "title": "Deskfish: Schedule a Task…"
      },
      {
        "command": "deskfish.keepRunning",
        "title": "Deskfish: Keep Running When VS Code Is Closed"
      },
      {
        "command": "deskfish.scheduledTasks",
        "title": "Deskfish: Scheduled Tasks…"
      },
      {
        "command": "deskfish.exportMemory",
        "title": "Deskfish: Export Her Memory…"
      },
      {
        "command": "deskfish.importMemory",
        "title": "Deskfish: Import Her Memory…"
      },
      {
        "command": "deskfish.openPlaybook",
        "title": "Deskfish: Open Her Playbooks"
      },
      {
        "command": "deskfish.editCharter",
        "title": "Deskfish: Edit Her Charter"
      },
      {
        "command": "deskfish.pastChats",
        "title": "Deskfish: Past Chats…",
        "icon": "$(history)"
      },
      {
        "command": "deskfish.settings",
        "title": "Deskfish: Settings…"
      },
      {
        "command": "deskfish.herFiles",
        "title": "Deskfish: Her Files…"
      },
      {
        "command": "deskfish.deletePastChats",
        "title": "Deskfish: Delete Past Chats"
      },
      {
        "command": "deskfish.setGatewayToken",
        "title": "Deskfish: Set Gateway Token"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "deskfish.newChat",
          "when": "view == deskfish.chat",
          "group": "navigation@1"
        },
        {
          "command": "deskfish.pastChats",
          "when": "view == deskfish.chat",
          "group": "navigation@2"
        },
        {
          "command": "deskfish.editMemory",
          "when": "view == deskfish.chat",
          "group": "1_memory@1"
        },
        {
          "command": "deskfish.clearMemory",
          "when": "view == deskfish.chat",
          "group": "1_memory@2"
        },
        {
          "command": "deskfish.openDocs",
          "when": "view == deskfish.chat",
          "group": "2_help@1"
        },
        {
          "command": "deskfish.showLog",
          "when": "view == deskfish.chat",
          "group": "2_help@2"
        },
        {
          "command": "deskfish.showSelf",
          "when": "view == deskfish.chat",
          "group": "1_memory@3"
        },
        {
          "command": "deskfish.openJournal",
          "when": "view == deskfish.chat",
          "group": "1_memory@4"
        },
        {
          "command": "deskfish.reflect",
          "when": "view == deskfish.chat",
          "group": "1_memory@5"
        },
        {
          "command": "deskfish.scheduledTasks",
          "when": "view == deskfish.chat",
          "group": "1a_schedule@1"
        },
        {
          "command": "deskfish.herFiles",
          "when": "view == deskfish.chat",
          "group": "1_memory@0"
        },
        {
          "command": "deskfish.settings",
          "when": "view == deskfish.chat",
          "group": "0_model@3"
        },
        {
          "command": "deskfish.exportMemory",
          "when": "view == deskfish.chat",
          "group": "1_memory@6"
        },
        {
          "command": "deskfish.importMemory",
          "when": "view == deskfish.chat",
          "group": "1_memory@7"
        },
        {
          "command": "deskfish.openPlaybook",
          "when": "view == deskfish.chat",
          "group": "1_memory@8"
        },
        {
          "command": "deskfish.editCharter",
          "when": "view == deskfish.chat",
          "group": "1_memory@9"
        },
        {
          "command": "deskfish.deletePastChats",
          "when": "view == deskfish.chat",
          "group": "1_memory@10"
        },
        {
          "command": "deskfish.changeModel",
          "when": "view == deskfish.chat",
          "group": "0_model@1"
        },
        {
          "command": "deskfish.setApiKey",
          "when": "view == deskfish.chat",
          "group": "0_model@2"
        },
        {
          "command": "deskfish.restartDesktop",
          "when": "view == deskfish.chat",
          "group": "2_help@0"
        }
      ]
    },
    "configuration": {
      "title": "Deskfish",
      "properties": {
        "deskfish.provider": {
          "type": "string",
          "enum": [
            "anthropic",
            "openai-compatible",
            "mock"
          ],
          "enumDescriptions": [
            "Anthropic API directly, using Claude's native computer-use tool (best click accuracy with Claude). The default.",
            "Any OpenAI-compatible chat/completions endpoint with vision + tool calling: LiteLLM proxy, xAI, OpenRouter, Ollama, vLLM…",
            "Scripted fake model for testing the plumbing without an API key."
          ],
          "default": "anthropic",
          "description": "Which model adapter drives the desktop. Anthropic direct is the default; it pairs with the default model, claude-opus-5."
        },
        "deskfish.autonomy": {
          "type": "string",
          "enum": [
            "free",
            "guided"
          ],
          "enumDescriptions": [
            "The tank is the boundary. Inside it the agent is free: it may use any account or saved login in its browser, complete purchases you ask for, and act without asking first. It still hands over when it needs something only you have.",
            "Cautious: the agent asks before anything irreversible (paying, sending, posting, deleting), stops before the final Pay click, and never uses credentials you did not give it."
          ],
          "default": "free",
          "description": "How much the agent may do on its own inside its tank."
        },
        "deskfish.userName": {
          "type": "string",
          "default": "",
          "description": "Your name, as the agent should know the person in the chat (e.g. \"Iman\"). Told to the agent at the start of every task so it never mistakes you for a third party. Leave empty to stay anonymous."
        },
        "deskfish.baseUrl": {
          "type": "string",
          "default": "",
          "description": "Base URL of the model endpoint. Required for provider=openai-compatible (LiteLLM: http://localhost:4000/v1, xAI: https://api.x.ai/v1, Ollama: http://localhost:11434/v1, OpenRouter: https://openrouter.ai/api/v1). For provider=anthropic leave empty (api.anthropic.com) unless you use a gateway."
        },
        "deskfish.anthropicWorkspaceId": {
          "type": "string",
          "default": "",
          "description": "Anthropic workspace ID (wrkspc_…), sent as the anthropic-workspace-id header. Needed only when your Anthropic API key is identity-linked (the API answers \"anthropic-workspace-id is required\"). Find it in the Anthropic Console under Settings → Workspaces. Leave empty for a workspace-scoped key."
        },
        "deskfish.model": {
          "type": "string",
          "default": "claude-opus-5",
          "description": "Model name exactly as the provider expects it (e.g. claude-opus-5, grok-4, llama3.2-vision)."
        },
        "deskfish.maxSteps": {
          "type": "number",
          "default": 0,
          "description": "Cap on model turns per task. 0 (default) = no cap: the agent works until the task is done or you press Stop, and the status row shows the running cost. A positive number stops the task at that many steps with a summary; \"continue\" resumes."
        },
        "deskfish.maxCostUsd": {
          "type": "number",
          "default": 0,
          "description": "Cost budget per task in US dollars: at list prices for Claude, Kimi or Grok used directly, or the cost OpenRouter reports. 0 = none. At 80% the agent is told to wrap up; at the budget it summarizes and stops, and \"continue\" resumes."
        },
        "deskfish.unattendedMaxCostUsd": {
          "type": "number",
          "default": 2,
          "description": "Cost budget in US dollars for a run nobody asked for and nobody is watching — a scheduled task. It also runs as \"guided\" unless the schedule says otherwise. 0 = no budget. A schedule can carry its own budget, which wins. Like deskfish.maxCostUsd, it can only act where the cost is known (list prices for Claude, Kimi or Grok used directly, or a cost the endpoint reports)."
        },
        "deskfish.screenshotWidth": {
          "type": "number",
          "default": 1280,
          "description": "Screenshots are downscaled to this width (keeping aspect ratio) before being sent to the model. Coordinates are mapped back automatically."
        },
        "deskfish.settleMs": {
          "type": "number",
          "default": 800,
          "description": "Milliseconds to wait after a batch of actions before taking the next screenshot."
        },
        "deskfish.desktop.daemonUrl": {
          "type": "string",
          "default": "http://localhost:9990",
          "description": "Base URL of the Bytebot desktop daemon (bytebotd)."
        },
        "deskfish.desktop.vncUrl": {
          "type": "string",
          "default": "ws://localhost:9990/websockify",
          "description": "WebSocket URL of the desktop's VNC (websockify) endpoint, used by the live desktop pane."
        },
        "deskfish.desktop.vncPassword": {
          "type": "string",
          "default": "",
          "description": "VNC password, if the desktop requires one."
        },
        "deskfish.desktop.token": {
          "type": "string",
          "default": "",
          "description": "Bearer token for the desktop daemon (DAEMON_TOKEN in the container). Also attached to the VNC websocket. Empty = no auth (fine when the port is bound to 127.0.0.1)."
        },
        "deskfish.desktop.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Turn the desktop on automatically when the Deskfish view opens. (Running a task always turns it on if needed.)"
        },
        "deskfish.desktop.openOnRun": {
          "type": "boolean",
          "default": true,
          "description": "Open the Desktop tab (the live view of the bot's screen) whenever you send a task, so you can watch it work. The chat keeps keyboard focus."
        },
        "deskfish.desktop.screen": {
          "type": "string",
          "default": "1280x800x24",
          "description": "Screen size of the bot's desktop (Xvfb geometry WIDTHxHEIGHTxDEPTH). Applied the next time the desktop is turned on."
        },
        "deskfish.desktop.composeFile": {
          "type": "string",
          "default": "",
          "description": "Optional compose file for people who prefer compose (docker/docker-compose.yml). The power button does not use compose."
        },
        "deskfish.desktop.containerCli": {
          "type": "string",
          "enum": [
            "auto",
            "docker",
            "podman"
          ],
          "default": "auto",
          "description": "Container CLI for the Start/Stop Desktop commands. auto = podman if installed and docker is not, else docker."
        },
        "deskfish.reflectEvery": {
          "type": "number",
          "default": 5,
          "description": "After how many finished tasks the agent reflects: alone with its journal and notes, it saves facts and playbooks it learned and may revise its own self file (who it is). It also reflects sooner when the tasks were weighty (long, costly, a hand-over). 0 = only when you run \"Deskfish: Let Her Reflect\"."
        },
        "deskfish.ledgerEvery": {
          "type": "number",
          "default": 40,
          "description": "Every this many steps of a long task, the agent writes a short ledger (goal, done, left, current state, traps) and the conversation restarts from that ledger and a fresh screenshot. Keeps the cost of each step flat and keeps the thread on long tasks. 0 = never condense."
        },
        "deskfish.scheduleGraceMinutes": {
          "type": "number",
          "default": 5,
          "description": "A scheduled task still starts this many minutes after its due time when Deskfish only just started (VS Code was opened a little late). Later than that, the occurrence is reported as missed and skipped. A task that was due while the agent was busy always runs once she is free."
        },
        "deskfish.ledgerTokens": {
          "type": "number",
          "default": 100000,
          "description": "Also write a ledger and restart the conversation when the last request's context (input plus cache) passed this many tokens, whatever the step count. Keeps a text-heavy task (many page reads, long command output) from paying for a huge context on every step. 0 = only by step count."
        },
        "deskfish.cacheTtl": {
          "type": "string",
          "enum": [
            "1h",
            "5m"
          ],
          "default": "1h",
          "description": "Anthropic prompt cache lifetime. 1h: the cached conversation survives the pauses between your messages, a standby and a slow command, at 2× instead of 1.25× the input price on each step's few new tokens. 5m: the cheaper write, for back-to-back requests only; after five idle minutes the whole conversation is re-written at cache-write price."
        },
        "deskfish.effort": {
          "type": "string",
          "enum": [
            "",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "",
          "description": "Anthropic only: how hard the model thinks on each turn. Empty = the provider's default (high). Lower effort means fewer thinking tokens, faster turns and fewer, more consolidated actions; medium is worth measuring on your own tasks. Applies from the next new conversation."
        },
        "deskfish.promptCaching": {
          "type": "string",
          "enum": [
            "auto",
            "on",
            "off"
          ],
          "enumDescriptions": [
            "Add cache breakpoints when the endpoint is openrouter.ai (Anthropic and Gemini models get prompt caching there); nothing for other endpoints.",
            "Always add them: for gateways that pass cache_control through (LiteLLM proxy in front of Anthropic).",
            "Never add them: for strict endpoints that reject unknown fields."
          ],
          "default": "auto",
          "description": "Prompt caching on OpenAI-compatible endpoints. Marks the system prompt and the newest message so the provider caches the conversation prefix (much cheaper long tasks). The Anthropic provider always caches."
        },
        "deskfish.temperature": {
          "type": [
            "number",
            "null"
          ],
          "default": null,
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature for OpenAI-compatible endpoints. Leave unset (null): no temperature is sent and every model runs at its provider default, which reasoning models such as kimi-k3 and GPT-5 insist on. Set a number (0 for the most repeatable clicks) only for models that accept one. The Anthropic provider uses adaptive thinking, which fixes its temperature."
        },
        "deskfish.gateway.placement": {
          "type": "string",
          "enum": [
            "local",
            "remote"
          ],
          "enumDescriptions": [
            "Deskfish runs on this computer: the extension starts it in the background and it keeps working when VS Code is closed. The default.",
            "Deskfish runs on another machine: set deskfish.gateway.url and enter its token with \"Deskfish: Set Gateway Token\"."
          ],
          "default": "local",
          "description": "Where the Deskfish gateway runs: the process that owns the desktop, the agent, her memory, chats and schedules. VS Code is one client of it. Reload the window after changing."
        },
        "deskfish.gateway.url": {
          "type": "string",
          "default": "",
          "description": "Address of a remote gateway (placement remote), e.g. http://127.0.0.1:9980 through an SSH tunnel or a Tailscale address. Empty with placement local: this computer, port 9980. Reload the window after changing."
        },
        "deskfish.gateway.keepRunning": {
          "type": "boolean",
          "default": false,
          "description": "Start Deskfish when you log in, so her scheduled tasks run and an interrupted task can be picked up after a restart. Turn it on with \"Deskfish: Keep Running When VS Code Is Closed\", which writes the entry (an autostart entry on Linux, a launch agent on macOS, a logon task on Windows) and shows it to you in a terminal; the same command removes it. Without it, Deskfish still keeps running after VS Code is closed — until the computer restarts."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "docs": "node scripts/build-docs.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "mock-daemon": "node scripts/mock-daemon.mjs",
    "smoke": "node dist/smoke.js",
    "package": "npx vsce package --allow-missing-repository --out deskfish.vsix && node scripts/check-vsix.mjs",
    "test": "node test/run.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pngjs": "^6.0.5",
    "@types/vscode": "^1.95.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.24.2",
    "tsx": "^4.23.13",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@novnc/novnc": "^1.5.0",
    "jpeg-js": "^0.4.4",
    "pngjs": "^7.0.0",
    "ws": "^8.21.3"
  },
  "author": {
    "name": "Iman Reihanian"
  }
}
