// Deskfish autoconfig. The first line of this file is skipped by Firefox — keep this comment here.
// The page bridge extension is built in the tank and unsigned; allow it.
lockPref("xpinstall.signatures.required", false);
lockPref("extensions.langpacks.signatures.required", false);
