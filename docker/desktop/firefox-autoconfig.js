// Deskfish: point Firefox at firefox.cfg (autoconfig). Needed so the unsigned Deskfish page bridge
// extension can be force-installed by policy. The file name must match general.config.filename.
pref("general.config.filename", "firefox.cfg");
pref("general.config.obscure_value", 0);
pref("general.config.sandbox_enabled", false);
